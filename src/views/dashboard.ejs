<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Calenda - Dashboard</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="css/dashboardStyle.css" />
</head>
<body>
  <header>
    <div id="logo">
      <img src="/images/certifica_logo.png" alt="CALENDA LOGO" />
    </div>
    <div class="user-info">
      <span>Bem-vindo, <%= user.nome %></span>
    </div>
  </header>

  <div class="main-container">
    <aside class="sidebar">
      <nav>
        <ul>
          <li><a href="/dashboard" class="nav-link active" data-section="todos-eventos">Todos os Eventos</a></li>
          <li><a href="#" class="nav-link" data-section="inscritos">Inscritos</a></li>
          <li><a href="#" class="nav-link" data-section="meus-eventos">Meus Eventos</a></li>
          <li><a href="/usuarios/perfil" class="nav-link" data-section="perfil">Perfil</a></li>
          <li><a href="/logout" class="nav-link logout">Logout</a></li>
        </ul>
      </nav>
    </aside>

    <main class="content">
      <div id="todos-eventos" class="section active">
        <h1>Todos os Eventos</h1>
        <div class="events-grid" id="events-container">
          <!-- Eventos serão carregados aqui via JavaScript -->
        </div>
      </div>

      <div id="inscritos" class="section">
        <h1>Eventos Inscritos</h1>
        <div class="events-grid" id="inscribed-events-container">
          <!-- Eventos inscritos serão carregados aqui via JavaScript -->
        </div>
      </div>

      <div id="meus-eventos" class="section">
        <h1>Meus Eventos</h1>
        <div class="my-events-header">
          <button id="create-event-btn" class="create-btn" style="display: none;">Criar Novo Evento</button>
          <div id="no-entity-message" style="display: none;">
            <p>Se vincule a uma entidade para criar eventos.</p>
          </div>
        </div>
        <div class="events-grid" id="my-events-container">
          <!-- Meus eventos serão carregados aqui via JavaScript -->
        </div>
      </div>

      <div id="perfil" class="section">
        <h1>Perfil</h1>
        <div class="profile-container">
          <form id="profile-form">
            <div class="form-group">
              <label for="nome">Nome:</label>
              <input type="text" id="nome" name="nome" value="<%= user.nome %>" required>
            </div>
            <div class="form-group">
              <label for="email">Email:</label>
              <input type="email" id="email" name="email" value="<%= user.email %>" required>
            </div>
            <button type="submit">Atualizar Perfil</button>
          </form>
          
          <div class="entities-section">
            <h3>Vincular-se a Entidades</h3>
            <div class="entity-form">
              <select id="entity-select">
                <option value="">Selecione uma entidade</option>
              </select>
              <input type="text" id="cargo" placeholder="Cargo na entidade" required>
              <button id="link-entity-btn">Vincular</button>
            </div>
            
            <h3>Entidades Vinculadas</h3>
            <div id="linked-entities">
              <!-- Entidades vinculadas serão carregadas aqui -->
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>

  <!-- Modal para detalhes do evento -->
  <div id="event-modal" class="modal">
    <div class="modal-content">
      <span class="close">&times;</span>
      <div id="event-details">
        <!-- Detalhes do evento serão carregados aqui -->
      </div>
    </div>
  </div>

  <footer>© 2025 Calenda - Todos os direitos reservados.</footer>

  <script src="js/dashboard.js"></script>
</body>
</html>

